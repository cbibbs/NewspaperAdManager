<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.bibbs.newspaperadmanager" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".main"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddNewspaper"
            android:label="@string/title_activity_add_newspaper"
            android:parentActivityName=".main" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="net.bibbs.newspaperadmanager.main" />
        </activity>
        <activity
            android:name=".AddAdvertisement"
            android:label="@string/title_activity_add_advertisement"
            android:parentActivityName=".main" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="net.bibbs.newspaperadmanager.main" />
        </activity>
    </application>

</manifest>
